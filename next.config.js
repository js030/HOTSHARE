/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'kr.object.ncloudstorage.com',
            'blog.kakaocdn.net',
            'www.lottehotel.com',
        ],
    },
}

module.exports = nextConfig
